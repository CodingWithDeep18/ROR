<h1>Products</h1>

<ul>
  <% @products.each do |product| %>
    <li><%= product.name %></li>
    <%= link_to "Delete", product_path(product), data: {turbo_method: :delete} %>
  <% end %>
</ul>

<%= button_to "New Products", new_product_path, method: :get %>
